"use client";
import React from "react";
import ContentPage from "../../../components/ContentPage";

const EstimacionCostos = () => {
  return (
    <ContentPage>
      Estimación de Costos para el sistema de integración UPB Datos de uso
      Modelo intermedio Effort Adjustment Factor EAF = EMi Effort
      Effort(E)=a(kLOC)b*EAF Time Time(T)=c(E)d Person-Month PM=Effort(E)Time(T)
    </ContentPage>
  );
};

export default EstimacionCostos;
